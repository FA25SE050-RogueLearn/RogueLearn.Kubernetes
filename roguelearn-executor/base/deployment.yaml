apiVersion: apps/v1
kind: Deployment
metadata:
  name: roguelearn-executor-api
  labels:
    app: roguelearn-executor-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: roguelearn-executor-api
  template:
    metadata:
      labels:
        app: executor
        component: backend
    spec:
      serviceAccountName: executor-sa
      containers:
        - name: executor
          image: songphuc/executor:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8081 # receive here
              protocol: TCP
            - name: grpc
              containerPort: 8082 # receive here
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthcheck
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
