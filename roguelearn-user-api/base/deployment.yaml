apiVersion: apps/v1
kind: Deployment
metadata:
  name: roguelearn-user-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: roguelearn-user-api
  template:
    metadata:
      labels:
        app: roguelearn-user-api
        component: backend
    spec:
      imagePullSecrets:
      - name: harbor-image-pull-creds
      containers:
      - name: roguelearn-container
        image: harbor.roguelearn.site/roguelearn/roguelearn-user-api:latest
        ports:
        - containerPort: 80
          name: http
        - containerPort: 443
          name: https
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:80;https://+:443"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5